{
  "main": "./entryPointForElectron.js",
  "type": "module",
  "scripts": {
    "bundle": "npx esbuild ./src/main.ts --bundle --platform=node --outfile=./dist/main.js",
    "dev": "npm run bundle && electron ."
  },
  "dependencies": {
    "electron": "*",
    "instrumentality": "*"
  },
  "devDependencies": {
    "typescript": "*"
  }
}
